## Running Locally

Build then start:

```bash
yarn build
yarn start
```

This runs node dist/index.js, which instantiates and starts ExampleServer.

To disable auto start (e.g. for tests):

```
AUTO_START=false node dist/index.js
```
